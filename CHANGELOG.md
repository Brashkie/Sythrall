# Changelog

Todos los cambios notables de Sythrall se documentan acá. Formato basado en
[Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## [Unreleased]

## [4.9.0] — 2026-08-18

### Added
- **Logo propio, reemplaza el emoji 🛰 en toda la app y los READMEs**: recortado y con el fondo negro removido (`apps/web/public/sythrall-logo.png`, transparencia real vía numpy — alpha en rampa según brillo del pixel en vez de un corte binario, para no dejar bordes dentados en las puntas de los triángulos) a partir del archivo original del usuario (`media/sythrall.png`, sin tocar). Reemplaza el favicon (`data:image/svg+xml` con el emoji dibujado adentro → `favicon-{32,64,128,256,512}.png` generados del mismo recorte), el logo del nav-rail, y el logo del topbar (nuevo — antes ese lugar no tenía ícono, solo texto). `.logo-ring` pasa de "caja con gradiente + emoji centrado" a simplemente la imagen (ya trae su propio color), mismo tamaño en los 3 breakpoints que ya la usaban. Agregado también arriba del título en ambos READMEs.
- **READMEs reescritos para lectura enterprise, roadmap movido a su propio doc**: `README.md`/`README.es.md` tenían la sección de roadmap (~325 líneas) escrita como diario de ingeniería — justificación completa, asides parentéticos, historial de auditorías por cada fase — mezclada con la documentación de instalación/API/referencia que un visitante nuevo realmente necesita. Esa sección se movió íntegra (sin perder contenido) a `ROADMAP.md`/`ROADMAP.es.md` nuevos; el README se queda con una tabla compacta de 25 fases + estado + link al roadmap completo. De paso: `README.md` ganó las secciones `Configuration`/`Troubleshooting` que solo existían en la versión en español (paridad entre idiomas), se sacó el emoji decorativo de headers/badges/tablas en los 4 documentos (mismo criterio que la pasada de-emojify del frontend), y se actualizaron números de tests que habían quedado desactualizados (329→405 Python, +69 Rust) y el inventario de `apps/web/src`/`services/complexity/src` con los archivos agregados en las últimas fases (`dashboard.ts`, `utils/health.ts`, `security.rs`, `smells.rs`, `space.rs`).
- **Project Health dashboard + Findings agregados a nivel de proyecto**: `security_findings`/`structural_smells` (Fase 21/22) existían solo por archivo desde que se shippearon; `routers/static_analysis.py::parse_project` ahora los agrega a nivel de proyecto completo con el mismo patrón que ya usaban `big_o_distribution`/`wasm_candidates` (`[{**item, "file": r["filename"]} for r in results for item in r.get(...)]`) — agregación aritmética pura sobre resultados ya calculados por el motor Rust-first, no lógica de análisis nueva. Reusa `routers/graph.py::_build_circular_graph` (sin duplicarlo) para una señal real de arquitectura. 4 scores, cada uno con su fórmula y sus números crudos visibles — nunca un número mágico:
  - `security = 100 - (high*15 + medium*5 + low*1)`
  - `quality = 100 - min(100, smells/(funcs+classes)*300)` — normalizado por tamaño de proyecto
  - `complexity = 100 - max(0, avg_cc-5)*8` — 5 como umbral "bueno", mismas bandas que radon usaba antes de la Fase 11
  - `architecture = 100 - cycles*15` — basado en el detector de dependencias circulares ya shippeado, en vez de inventar un número de "Performance" sin datos de runtime detrás

  Frontend: nuevo `panels/dashboard.ts` (el Dashboard nunca había tenido módulo propio) con `loadProjectHealth`/`renderProjectHealth`, y `utils/health.ts` con el renderer de las 4 tarjetas compartido entre el Dashboard y la vista de proyecto de Static (una sola implementación, dos lugares). Empty state accionable en ambos: sin proyecto activo → CTA a Proyectos; proyecto activo sin analizar → botón "Analizar salud del proyecto". `_renderProjectResult()` en `static.ts` gana una lista de Findings agregados (reusa `_renderSecurityFindings`/`_renderStructuralSmells`, con el archivo de origen mostrado por item, ordenados por severidad/tipo). `SecurityFinding`/`StructuralSmell` (antes duplicados de forma privada dentro de `panels/static.ts`) se promueven a `api/client.ts` como interfaces exportadas con `file?: string` opcional. Fix encontrado en verificación: `switchTab('dashboard')` no re-renderizaba Project Health al volver de otro tab — corregido con el mismo patrón de re-render por tab que ya usan `upload`/`static`. 7 tests nuevos (`TestProjectHealth`).
- **Sidebar IA regroup**: los 11 tabs de siempre (dashboard/editor/apis/issues/diagram/ml/metrics/diff/logs/upload/static) se reorganizan en 5 clusters lógicos — OVERVIEW (Dashboard), PROJECT (Editor/Proyectos/Diff), ANALYSIS (Static/Métricas/Diagrama/ML-DL), FINDINGS (Issues), SYSTEM (APIs/Logs, bajan de categoría) — separados por `.nav-rail-divider` en vez de headers de texto (72px de ancho del rail no da lugar sin wrap raro; mismo patrón que la activity bar de VS Code). Cero cambios de wiring: `switchTab()` en `app.ts` itera por id, no por orden de DOM. El menú "Más" del bottom-nav gana headers reales (`.bn-more-section-label`) agrupando Project/Analysis/System.
- **Empty states, de mudos a accionables**: `.empty` gana una variante con botón (`.empty .btn`) — Explorer y el panel Issues muestran un botón "+ Código" que dispara el flujo real de carga de archivos, no un mensaje mudo. Se encontró que el empty state de Issues era peor que mudo — literalmente en blanco, porque `renderIssuesList()` solo se llamaba después de correr un análisis completo — corregido llamándolo desde `initApp()`. `.exp-empty` (variante duplicada) se unifica en `.empty`.
- Ambas pasadas de UX verificadas end-to-end con Playwright contra el dev server real (no solo build/typecheck): los 11 tabs, el toggle de tema, el breakpoint <480px, y los CTAs de empty state confirmados funcionando.
- **Fase 22 — Structural smells, Rust y Python el mismo día**: `smells.rs`/`static_parser.py`, 5 chequeos con umbrales convencionales de Fowler/Martin — función larga (LOC>50), exceso de parámetros (>5), anidamiento profundo (cualquier tipo de bloque — if/for/while/with/try, no solo loops, profundidad>4), clase grande (métodos>15 O LOC>300), god object (métodos≥20 Y atributos propios≥10). Cada smell trae su umbral y su razonamiento en el mensaje, nunca una etiqueta sola — mismo estándar que ya se exige el motor de Big-O. Nuevo `count_self_attributes()`/`_count_self_attributes_python()` reusa el walker consciente de scope (`walk_stmts_own_scope`/`_own_scope_nodes`) construido para taint tracking en la Fase 21, para contar asignaciones `self.X = ...` en cualquier método sin filtrarse a funciones anidadas. "Lógica duplicada" (comparación de forma de AST entre funciones) queda explícitamente diferida — necesita un esquema de normalización/hashing que todavía no existe, documentado en el propio código como pendiente, no silenciado. `RichClass` gana `end_line`/`loc`/`attribute_count`; un objeto god-object-sized dispara `large_class` y `god_object` a la vez de forma esperada, ya que el umbral de métodos de god object (20) supera al de large class (15) — confirmado en la paridad manual entre ambos lenguajes. 11 tests unitarios nuevos en Rust + 14 en Python (`test_structural_smells.py`), paridad confirmada a mano sobre los mismos 5 casos sintéticos más un caso limpio. Cableado en `structural_smells` de `/static/parse` (los 7 shapes de retorno de `_parse_python()` actualizados para consistencia) y en el panel Static (`panels/static.ts`, nueva sección colapsable junto a Security & Taint).
- **Fase 22 — Halstead desglosado, primera porción, Rust-only**: `maintainability.rs::HalsteadMetrics`/`halstead_metrics()` expone los 5 componentes estándar (Vocabulario, Longitud, Volumen, Dificultad, Esfuerzo) que el Volumen de Halstead ya calculaba internamente para el MI desde la Fase 11, hasta ahora escondido dentro de la fórmula de Coleman-Oman. Sin fallback Python — `analyze_complexity()` no lo tenía para `mi`/`functions`/`raw` tampoco, mismo límite desde que `complexity-engine` reemplazó a `radon`; el shape vacío ahora trae `halstead: None` explícito. De paso, refactorizar `compute()` para que reciba las métricas ya calculadas en vez de re-recorrer el AST por su cuenta **eliminó un recorrido duplicado del archivo completo que existía antes de esta fase** — Criterion confirmó una mejora real de 4.6–7.1% en `analyze()` a 10/100 funciones (sin cambio significativo a 1000, ruido domina a esa escala). Cableado en `routers/analysis.py` (`/analyze/code`, `/analyze/project`) y `routers/intelligence.py` (`/intel/analyze`), 4 call sites actualizados consistentemente. Frontend: nuevo tipo `HalsteadMetrics` en `types/index.ts`, desglose colapsable debajo de la tarjeta de MI en `panels/analysis.ts`. 8 tests unitarios nuevos en Rust + 3 en Python confirmando que la key `halstead` está presente (en `None`) en el shape degradado. Fase 22 pasa de 🔴 a 🟡.
- **Fase 21 — Catálogo CWE v1 completo (5 de 5), Path Traversal y Deserialización insegura, Rust y Python el mismo día**: `check_path_traversal`/`check_insecure_deserialization` en `security.rs` + `_check_path_traversal`/`_check_insecure_deserialization` en `static_parser.py`. Path Traversal (CWE-22) — `open(path)` dispara solo si el path se arma por concatenación/f-string/`.format()` (mismo criterio "construido" que SQLi), `os.path.join(base, seg)` dispara con cualquier segmento tainted más allá del primero aunque no esté concatenado (unir un `"../etc/passwd"` crudo ya es el traversal). Deserialización insegura (CWE-502) — `pickle.loads`/`pickle.load`/`marshal.loads` disparan siempre (confianza Media sin taint provable, Alta si el argumento sí traza a una fuente), porque a diferencia de SQLi/command injection no existe una "forma segura" de esas llamadas; `yaml.load` dispara salvo `Loader=yaml.SafeLoader`/`CSafeLoader` explícito — primer uso real del nivel de confianza Media en el catálogo. `security_findings()` en ambos lenguajes ya no corta temprano cuando no hay taint (CWE-502 puede disparar sin taint). 14 tests nuevos en Rust + 14 en Python, paridad byte a byte confirmada a mano en los 7 casos base. Benchmarkeado con Criterion: 10 funciones — 0.52ms vs 15.05ms (28.9×); 100 — 7.16ms vs 152.21ms (21.3×); 1000 — 197.23ms vs 1581.21ms (8.0×). Fase 21 pasa de 🟡 a ✅ — todos los ítems del catálogo v1 shippeados en ambos lenguajes.

- **De-emojify de toda la UI**: auditoría completa de `apps/web/src` (20 archivos `.ts`) + `index.html` — 249 + 24 ocurrencias de emoji encontradas y resueltas con 3 estrategias según el caso, no un reemplazo 1:1 por ícono: (1) badges de lenguaje (🐍🟦🟨⚙️, 3 copias casi idénticas del mismo mapa en `static.ts`/`graph.ts`/`app.ts`) consolidados sobre `languageBadge()` (`utils/icons.ts`), que ya existía para esto pero nunca se conectó; (2) severidad/estado (🔴🟡🔵✅⚠️🔒) reemplazados por el patrón de punto de color ya establecido (`dot-ok`/`dot-warn`/`dot-err`) o por `icon('warning'|'check'|'shield')` — 3 íconos nuevos en `icons.ts`, el de `warning` reusando el path que ya existía hardcodeado en `index.html` (tab Problems) en vez de dibujarlo de cero; (3) el resto (📏🎛️🪆🏛👹💡🔥🚀🤖⚡🌐☕🐹🎨 — labels de structural smells, CTAs, sugerencias, badges de clasificadores CS Engine) sacados sin reemplazo, texto solo, porque eran puramente decorativos. Se dejan a propósito los glyphs tipográficos que ya eran monocromos antes de esta pasada (`✓✗⚠✕✔⬇·◌` en `LOG_ICONS`/`flow.ts::ICONS`/badges de acción) y el logo/favicon (🛰, marca, no UI incidental). De paso, `NotebookEnv.icon` (tipo + 3 usos, dato 100% frontend) se elimina del tipo; `PipelineStep.icon` se deja intacto porque lo puebla el backend, fuera de alcance de esta pasada. Bug real encontrado en el camino: el FAB `.rp-fab` (abre el panel derecho en mobile) tenía un SVG correcto en el HTML inicial pero `events.ts` lo pisaba con un emoji `⚡` apenas se tocaba el botón — corregido para usar el mismo ícono SVG (`icon('metrics', ...)`) en ambos estados.
- **Fixes de responsive, auditoría completa de los 16 breakpoints existentes**: `.ac-metrics` (panel APIs) tenía una regresión real — pasaba de 3 a 2 columnas a 680px pero VOLVÍA a 3 a 480px (pantalla más angosta, más columnas) — corregido a 2/2. Diff y Logs, que no tenían CSS responsive propio, ganan: `#diff-out`/`.ls-row` con `overflow-wrap`/`min-width:0` (el mensaje de log podía empujar el ancho de la fila en vez de wrappear), tamaños de fuente ajustados a 680/480px. Editor gana `min-height` en `#editor-container` a 680/480px. `.bigo-notation-table` (Static) gana wrapper `overflow-x:auto`, mismo patrón que la tabla de Big-O principal. Encontrado durante la verificación visual (no por el chequeo automático de overflow, que no lo detecta): el header "PROJECT HEALTH" del Dashboard se cortaba a 375-680px porque `.cc-head` es flex sin wrap y el subtítulo decorativo empujaba más allá del `overflow:hidden` del card — corregido ocultando el subtítulo (`.cc-head-sub`) bajo 680px en vez de dejarlo cortado a la mitad.
- Ambas pasadas verificadas con Playwright contra el dev server real en 5 viewports (375/480/768/900/1440px) × 6 paneles representativos (incluyendo el patrón correcto de navegación mobile — bottom-nav + overflow "Más" para paneles no primarios, no los ids de nav-rail que están `display:none` bajo 480px): cero overflow horizontal, cero emoji residual, cero errores de página.

### Explicitly deferred
- Tab "Issues" → "Findings" (pipeline de datos distinto — flake8/pylint, no el motor estático; fusionar de verdad los dos pipelines es su propio trabajo), páginas dedicadas de Security/Quality/Architecture como nav items, Inspector contextual, Platform/Plugins, AI Insights — siguen necesitando infraestructura que no existe.

### Removed
- **6 instancias de código muerto encontradas por una auditoría dirigida, cada una verificada a mano (grep de árbol completo, no solo el reporte del agente) antes de tocar nada**:
  - `apps/web/src/api/client.ts`: 7 métodos wrapper `intelLint`/`intelAnalyze`/`intelHover`/`intelDefinition`/`intelReferences`/`intelCompletions`/`intelRename` — duplicaban a mano lo que `editor-intelligence.ts` ya hace con `fetch()` directo contra los mismos endpoints `/intel/*`. Junto con sus 6 tipos asociados (`IntelMarker`, `IntelLintResult`, `IntelAnalyzeResult`, `IntelHoverResult`, `IntelDefinitionResult`, `IntelReferencesResult`) — confirmados sin ningún import fuera de su propia declaración.
  - `apps/web/src/api/client.ts`: `staticBigO`/`staticWasm`/`staticLanguages` — cero callers; `panels/static.ts` obtiene esos mismos datos vía `staticParse`/`staticParseProject`. `BigOResult` eliminado junto con `staticBigO` (sin otros usos); `WasmHint` se queda — lo siguen necesitando `StaticParseResult`/`StaticProjectResult`, que sí se usan. Los endpoints de backend (`/static/bigO`, `/static/wasm`, `/static/languages`) NO se tocaron — tienen tests propios y `/static/bigO` es la pieza que Fase 18 ya benchmarkeó contra Rust; es superficie sin UI, no código muerto.
  - `apps/web/src/store/state.ts::removeFile` — export sin ningún importador; `app.ts` usa su propia función local del mismo nombre (con más limpieza: `explorerRemoveFile`, `updateFileTree`, `updateStats`), no la del store. Mismo patrón que el `findFile` ya eliminado.
  - `apps/api/services/project_service.py::save_files()` — sin ningún caller ni siquiera dentro de su propio módulo; `routers/upload.py` persiste archivos con su propia lógica inline.
  - `POST /metrics/validate` y `GET /api/history` — confirmados sin ningún caller de frontend, pero **NO eliminados**: el primero tiene 7 tests propios (validación de binario/truncamiento/encoding/sintaxis), el segundo es un endpoint chico con test dedicado — mismo criterio que Force Graph/Dir Tree, superficie implementada y testeada sin trigger de UI todavía, no cruft accidental. Documentados acá para que quede constancia, no borrados por incertidumbre sobre si son superficie de API pensada para algo que no aterrizó todavía.

## [4.8.0] — 2026-08-18

### Added
- **`routers/graph.py::_build_centrality_graph` — Centralidad/detección de hubs (Fase 14)**: nuevo tipo de grafo `centrality` en `/analyze/graph` y `/analyze/graph/project`, reusando el import graph que ya construyen los otros 4 tipos y NetworkX (`nx.degree_centrality`/`in_degree`/`out_degree` sobre el mismo `DiGraph` que ya usa el detector de dependencias circulares — no una librería nueva). Un archivo se marca "hub" si está entre los 5 de mayor in-degree y tiene ≥2 dependientes. Cableado end-to-end: nueva opción en el dropdown de proyecto del panel Diagrama (`index.html`), `centrality` agregado a `PROJECT_GRAPH_TYPES` en `app.ts`, mensajes de status en `panels/graph.ts` para ambas rutas (sidebar y proyecto subido) — no solo una respuesta de API sin UI, la misma lección que dejó el hueco del Force Graph. 8 tests nuevos (`TestCentralityGraph`) + fix de un test existente que asumía "siempre 4 tipos de grafo" (ahora 5, con el nuevo id incluido en la lista verificada). Verificado en vivo con curl (archivo con 3 dependientes correctamente marcado como hub, archivo con 1 dependiente correctamente no marcado).
- **`space.rs`/`_infer_space_python` — Complejidad de espacio (Fase 13), construido en Rust y Python el mismo día**: heurística AST paralela al motor de Big-O de tiempo, pero mirando qué estructura auxiliar se construye (no cuántas veces itera un loop) — un acumulador escalar en un loop es O(1) de espacio aunque sea O(n) de tiempo. Detecta colecciones que crecen (`.append`/`.add`/`.update`/`.insert`/`.extend`, o `d[k]=v`) por profundidad de anidamiento de loop, comprehensions anidadas, y profundidad de call stack por recursión (O(log n) binaria, O(n) lineal). Nuevo `pub(crate) fn has_binary_split` en `bigo.rs` (antes privada) para reusar el mismo detector de split en ambos motores. 8 tests en Rust + 8 en Python (`TestSpaceComplexity`), paridad byte a byte confirmada a mano. Cableado en `RichFunction`/`analyze_rich()`, `/intel/hover`, `/intel/analyze` (Rust primero, fallback Python) y una columna **Space** nueva en la tabla Big-O del panel Static. Benchmarkeado con Criterion contra `_parse_python()` real: 10 funciones — 0.77ms vs 18.0ms (23.4×); 100 — 9.17ms vs 212.6ms (23.2×); 1000 — 254ms vs 2083ms (8.2×) — reportado junto con la regresión honesta que este pase (dos recorridos completos más por función: estructuras auxiliares + profundidad de comprehensions) le agregó a `analyze_rich()` mismo: 30–59% más lento que su baseline previo, según el propio Criterion.
- **`security.rs` — Security & Taint Intelligence (Fase 21) portado a Rust, el mismo día que se shippeó en Python**: taint tracking (`taint_source_kind`/`expr_taint_info`/`taint_propagation`), SQL Injection (CWE-89), Command Injection (CWE-78) y credenciales hardcodeadas (CWE-798) — puerto 1:1 de la sección "SECURITY & TAINT" de `static_parser.py`, mismo estilo modular que `bigo.rs`/`classifiers.rs`/`recursion.rs`. Sin agregar el crate `regex` como dependencia nueva: los dos patrones que Python resuelve con regex (nombre de variable tipo credencial, valor tipo placeholder) se resuelven con matching de substrings plano, misma filosofía de dependencias mínimas que ya tiene el `Cargo.toml`. Nuevo `walk_stmts_own_scope` en `walk.rs` (variante de `walk_stmts` que no desciende a `FunctionDef`/`AsyncFunctionDef` anidados) para no reintroducir en Rust el bug de scope-bleed que se encontró y corrigió en la versión Python. Wireado en `RichAnalysisResult`/`analyze_rich()` → `/parse/python`, consumido por `/intel/hover` (Rust primero, fallback a Python igual que el resto de los clasificadores). 9 tests unitarios nuevos en Rust, paridad confirmada a mano contra los 21 tests de Python sobre los mismos casos. Benchmarkeado con Criterion contra `_parse_python()` real (que ahora también corre el pase de seguridad) sobre los archivos sintéticos de siempre: 10 funciones — 0.52ms vs 11.97ms (23×); 100 — 6.37ms vs 129.0ms (20.3×); 1000 — 191.7ms vs 1316.9ms (6.9×) — reportado junto con la regresión honesta que el pase de taint le agregó a `analyze_rich()` mismo (2.7–8% más lento que su baseline anterior, según el propio Criterion).
- **Fase 18 renombrada "Native Analysis Core" y redefinida como migración comprometida, no como techo aspiracional**: `static_parser.py` deja de ser "Python y Rust conviven para siempre" — el motor de análisis estático se muda a Rust por completo (parsing, AST, símbolos, complejidad, seguridad, grafos, calidad), consolidado progresivamente en un core modular (`Parsing`/`Analysis`/`Graph` → `Deep Analysis`: `Security`/`Quality`/`Performance`, mismo patrón de módulos separados que ya usan `bigo.rs`/`classifiers.rs`/`recursion.rs`/`structure.rs`, nunca un `static_parser.rs` monolítico). Estado final: sin capa de compatibilidad permanente, sin doble implementación — una vez portada y probada una pieza, la versión Python se elimina. Lo que se mantiene del proceso benchmark-first anterior es el *método* (cada porción se sigue portando, testeando por paridad, y benchmarkeando antes del swap), no la pregunta de *si* migrar — esa ya está resuelta, y se documenta como una excepción explícita y única a la regla general de "adoptar Rust solo si el benchmark lo justifica" que sigue aplicando a cualquier otra adopción de lenguaje nativo fuera de esta migración puntual. Tabla de filosofía de lenguajes actualizada para reflejar el rol de Rust como destino comprometido del análisis estático y el de Python retirándose hacia IA/ML/científico.

- **Roadmap ampliado con 3 fases nuevas, a partir de una discusión de arquitectura sobre extensibilidad y análisis profundo de software** (ver `README.md`/`README.es.md`):
  - **Fase 21 — Security & Taint Intelligence**: llena un hueco real que la reorganización a 9 pilares de v4.7.1 no cubrió — nada en el roadmap tocaba seguridad hasta ahora. Taint tracking dentro de una función (source→sink), catálogo CWE v1 (SQL/command injection, path traversal, deserialización insegura, secretos hardcodeados), findings con confianza puntuada (nunca binario) y un schema de finding en árbol de evidencia — mismo estilo heurístico honesto que ya usan los clasificadores de regex/grammar de la Fase 8/12, explícitamente no un reemplazo de SAST maduro.
  - **Fase 22 — Code Quality Intelligence**: desglosa el Volumen de Halstead que `maintainability.rs` ya calcula internamente para el MI (Coleman-Oman) en sus 5 componentes propios (Vocabulario/Longitud/Volumen/Dificultad/Esfuerzo), y agrega smells estructurales/de nombres/arquitectura que un solo número de MI no puede expresar — construido sobre el import graph que ya existe (Fase 6/14), no una arquitectura nueva.
  - **Fase 24 — Extensibility Platform**: el límite de API (manifest de plugin + interfaz de capability) que dejaría crecer las Fases 13–23 como plugins en vez de que cada analizador termine directo en `apps/api` — deliberadamente acotada a una primera porción sin marketplace/sandboxing/SDK multi-lenguaje, con una regla explícita de que el core sigue funcionando con cero plugins instalados. Incluye la separación plugin (analizador) vs. extension (UI) y un `AIProvider` opcional donde la IA solo explica evidencia ya determinista, nunca es la fuente de detección.
  - Fase 21 (antes Execution Intelligence) renumerada a **Fase 23**; Fase 22 (antes Sythrall Platform) renumerada a **Fase 25** — todas las referencias cruzadas de número de fase (tabla de filosofía de lenguajes, párrafo de overview del roadmap) actualizadas en ambos idiomas. Fases 1–20 y 23/25 (contenido) sin cambios, solo el número.

- **Fase 21 — Security & Taint Intelligence, primera porción implementada** (`services/static_parser.py`, sección nueva "SECURITY & TAINT"): taint tracking recursivo de una sola pasada por función (`_taint_propagation_python`/`_expr_taint_info`) que resuelve un valor hasta una fuente no confiable (HTTP request, `input()`, `sys.argv`, variables de entorno) a través de asignaciones/concatenación/f-strings/`.format()`, distinguiendo si el taint se armó por construcción de string (la señal real de riesgo) o solo pasó de largo — esa distinción es lo que hace que `cursor.execute("...%s...", (val,))` (parametrizado, seguro) no dispare mientras que `cursor.execute("..." + val)` (concatenado, vulnerable) sí. 3 CWEs del catálogo v1 shippeados: SQL Injection (CWE-89), Command Injection (CWE-78, `os.system`/`os.popen`/`subprocess.*(shell=True)`, ignora `subprocess.run([...])` sin `shell=True` — la forma segura), y credenciales hardcodeadas (CWE-798, heurística de nombre+forma a nivel de archivo). Path Traversal (CWE-22) y Deserialización insegura (CWE-502) quedan explícitamente para después. Wireado en `/static/parse` (nueva key `security_findings`, todos los shapes de todos los lenguajes actualizados para consistencia) y en el hover de Python (`_build_hover_python`, nueva sección `🔐`). Frontend: nueva sección en el panel Static (`_renderSecurityFindings`) arriba de la tabla Big-O, mismo patrón visual que los WASM hints. 21 tests nuevos (`test_security_findings.py`) + 2 tests de hover — cubren tanto el caso vulnerable como el seguro/parametrizado de cada check, no solo el positivo. Verificado en vivo vía curl a `/intel/hover`.

- **Fase 13 — Algorithmic Intelligence, primer ítem cerrado: referencia de notación asintótica.** El panel Static (`panels/static.ts::_renderBigOTable`) gana una tabla `<details>` colapsable arriba de la tabla Big-O con los 5 símbolos — O, Θ, Ω, o, ω — definición general primero, honesto en que `o`/`ω` no se calculan (sin heurística estática confiable que los distinga del caso ajustado/O). El hover de Python (`_build_hover_python`) recibe la misma explicación como nota al pie de una línea bajo su tabla O/Θ/Ω. El hover de JS/TS queda sin tocar a propósito — solo calcula O, la referencia completa no aplica todavía ahí. Nuevo test (`test_hover_notation_reference_footnote`) confirma el pie de nota; verificado también en vivo vía curl a `/intel/hover`.

### Fixed
- **5 bugs reales encontrados por una auditoría dirigida a las Fases 1–20 ya marcadas ✅/🟡** (agente de exploración, cada uno reproducido antes y después del fix, no solo leído):
  - **Taint tracking (Fase 21, recién agregado) no limpiaba el taint en una reasignación a valor seguro** — `cmd = request.args.get("x"); cmd = "ls -la"; os.system(cmd)` disparaba un falso positivo CWE-78 de confianza Alta, porque `_taint_propagation_python` solo actualizaba el dict cuando el nuevo RHS SÍ estaba tainted, nunca lo limpiaba cuando no. Corregido: ahora una reasignación a un valor no-tainted borra el taint previo de ese nombre.
  - **Taint tracking también se filtraba entre scopes**: una función anidada que reusaba el nombre de una variable del scope externo contaminaba el taint del scope externo, porque `ast.walk(func)` desciende a funciones/lambdas anidadas tratándolas como parte del mismo scope lineal. Nuevo helper `_own_scope_nodes()` — como `ast.walk` pero no baja a `FunctionDef`/`AsyncFunctionDef`/`Lambda` anidados (esas ya se analizan por separado cuando el loop principal las visita) — usado tanto en la propagación de taint como en la búsqueda de sinks. 2 tests de regresión nuevos.
  - **Complejidad ciclomática de C/C++ y JS contaba cada `else if` dos veces**: `body_src.count(" if ")` ya cuenta la ocurrencia de `" if "` dentro de `"else if"` como substring, y sumar `body_src.count(" else if ")` aparte la contaba otra vez — 1 `if` + 2 `else if` daba CC=6 en vez de CC=4. Corregido eliminando el conteo separado de `else if` en ambos lenguajes (`_ts_extract_functions` para C/C++, `_cyclomatic_js`). 2 tests de regresión nuevos.
  - **Estimador de LOC de funciones JS/TS absorbía la función siguiente** cuando la actual era una arrow function sin `{ }` (`const isEven = (n) => n % 2 === 0;`): al no haber ninguna llave que abrir, el scan seguía de largo por el resto del archivo hasta encontrar la primera `{` de la SIGUIENTE función, heredando su LOC/complejidad/Big-O (y en cascada, falsos positivos de WASM hot-path sobre one-liners triviales). `_estimate_js_func_loc` ahora corta en el primer `;` fuera de paréntesis visto antes de cualquier `{` — no cubre el caso sin punto y coma final (ASI), documentado como límite aceptado. 2 tests de regresión nuevos (incluye uno que confirma que una firma multi-línea con llave normal sigue midiéndose bien).
  - **`store/state.ts::findFile`** — export sin ningún caller en todo `apps/web/src` (grep de árbol completo), mismo patrón que los 3 exports que la Fase 12 ya había limpiado. Eliminado.
  - `README.md`/`README.es.md`: la Fase 6 marcaba "Complexity Heatmap" como shippeado sin la misma salvedad honesta que ya tenía el Force Graph — el dir-tree (`renderDirTree`) está tan desconectado de la UI como el Force Graph (`generateWholeProjectDiagram` en `app.ts` pasa `onDirTree` como no-op, documentado en su propio docstring desde antes). Ahora ambos comparten la misma nota de "implementado y testeado, sin control de UI todavía".
- `README.md`/`README.es.md`: el ítem "Problems panel placement" de la Fase 10 seguía marcado `[ ]` a pesar de que su propia nota decía "resuelto en la Fase 12" — desde v4.6.0 realmente lo estaba, solo faltaba tildar el checkbox.

### Changed
- **`parse_python_rich()` conectado en `/intel/analyze` y `/intel/hover`**, además de `/static/bigO` (v4.7.1): el "heavy path" que corre a los ~2s de inactividad mientras se tipea (`heavy_analyze`) y el endpoint de hover (`_build_hover_python`) reemplazan su bloque de `ast.parse`+`ast.walk`+8 llamadas a clasificadores por una llamada a `parse_python_rich()`, con la misma degradación con gracia sin gate cacheado ya establecida (`analyze_big_o` en v4.7.1). En hover, solo la porción de clasificación CS Engine se delega a Rust — la firma de función sigue construida con `ast.unparse()` en Python porque el `RichFunction` de Rust solo trae nombres de argumento planos, no anotaciones de tipo. Elimina una triplicación real: la misma lógica de Big-O/Θ/Ω/recursión/regex/grammar/graph vivía por separado en `static_parser.py`, `heavy_analyze` y `_build_hover_python`. Verificado en vivo con el sidecar arriba y abajo (texto de razón en inglés vs. español confirma qué path corrió), 329 tests de `pytest` sin cambios.
- `README.md`/`README.es.md`: la Fase 18 documenta ahora los 3 endpoints conectados (`/static/bigO`, `/intel/analyze`, `/intel/hover`), no solo el primero.
- `scripts/README.md`: referencias corregidas a la estructura actual (`apps/`+`services/`, no `backend/`/`frontend/`/`terminal-server/`), `cwd=apps/api/` corregido (decía `backend/`), y se documentó `run-complexity.mjs`/`dev:cx`, que faltaba.
- `explorer.ts`: comentario de cabecera reescrito — describía una guía de integración de 5 pasos con `explorerSelectFile` como API pública que nunca se adoptó (`app.ts` engancha la apertura de archivos vía el callback `onFileOpen`, no llamando a esa función directo).

### Removed
- **Limpieza de código muerto en TypeScript**, confirmado con grep en todo `apps/web/src` (cero callers, no solo cero imports) antes de cada borrado:
  - `editor-intelligence.ts`: `CallGraphData`, `fetchCallGraph`, `callGraphToMermaid`, `StaticParseForGraph` — unidad completa sin ningún caller, superseded por el pipeline de call graph ya conectado (`panels/graph.ts` + `/analyze/graph`).
  - `panels/graph.ts`: `getViewMode`/`setViewMode`/`getCurrentResult` y el estado que solo alimentaban (`_viewMode`, `_currentResult`).
  - `utils/helpers.ts`: `clamp`.
- **De-exportado** (la función se queda, solo se le saca `export`, porque solo se usa dentro de su propio archivo): `terminal.ts::openTerminalPanel`, `explorer.ts::toggleSearch`/`closeSearch`/`renderOutline`/`explorerSelectFile`, `editor-intelligence.ts::teardownIntelligence`, `app.ts::updateBadges`/`renderURLList`, `helpers.ts::nowTs`.

## [4.7.1] — 2026-08-17

### Changed
- **Roadmap reorganizado alrededor de "Computer Science Intelligence"**: las Fases 13-22 de `README.md`/`README.es.md` pasaron de una lista de features sueltas (Data Structure Detector, Language Intelligence, Cython & WASM, etc.) a 9 pilares conceptuales explícitos — Algorithmic Intelligence, Data Structures & Graph Intelligence, Mathematical Intelligence, Formal Language Intelligence, Compiler Intelligence, Native Intelligence, Machine Intelligence, Scientific Intelligence, Execution Intelligence — más una Fase 22 "Sythrall Platform" que junta todo lo que era puramente de producto/distribución (Zig, Cython/WASM, Execution Path Simulator, persistencia empresarial, VS Code/LSP/Jupyter/ApexVision). Motivo: la descripción honesta de Sythrall hoy es "lee código, calcula Big-O" — el objetivo pasa a ser conectar esa heurística con la teoría de CS que ya la explica (la jerarquía de Chomsky que los clasificadores de la Fase 8/12 ya usan, el framing de Cálculo Lambda que ya tiene la recursión tail-call), no agregar features sueltas. Fases 1-12 (historial ya shippeado) no se tocaron — solo se corrigieron las referencias cruzadas de número de fase que quedaron desactualizadas por el corrimiento (`Fase 15`→`18`, `17`→`19`, `18`→`20`, `19`→`22`).

### Added
- **Fase 1 de la migración de `static_parser.py` a Rust**: `services/complexity` gana 5 módulos nuevos (`walk.rs`, `bigo.rs`, `recursion.rs`, `classifiers.rs`, `structure.rs`, orquestados por `rich.rs`) que portan 1:1 todo lo que `_parse_python()` calcula por función/clase/import — Big-O, Θ/Ω, complejidad ciclomática, recursión tail-call, y los 3 clasificadores del CS Engine (regex/grammar/graph traversal) de la Fase 12. Expuesto como `POST /parse/python` en el sidecar existente (no uno nuevo). `static_parser.py` no se tocó — Python y Rust corren en paralelo, exactamente como pidió el usuario ("no reemplazaría static_parser.py de golpe").
- `walk.rs`: walker genérico y exhaustivo del AST (equivalente real a `ast.walk()` de Python — visita cada statement/expresión sin importar la profundidad de anidamiento), compartido por los 4 módulos nuevos en vez de que cada uno reinvente su propio recorrido parcial (el primer intento de `recursion.rs` sí lo hacía, con walkers incompletos que se descartaron antes de mergear).
- `services/complexity_client.py::parse_python_rich()`: cliente del endpoint nuevo, `None` en cualquier falla (el caller decide caer a `_parse_python()`).
- **Benchmarkeado con Criterion, no asumido**: `services/complexity/benches/parse_bench.rs` vs. `_parse_python()` real cronometrado con `timeit` sobre los mismos archivos sintéticos (10/100/1000 funciones, ahora con regex + recursión + loop, no solo el caso simple). Resultado: 0.48ms vs 9.94ms (10 funciones, 20.6×), 5.95ms vs 100.7ms (100, 16.9×), 187ms vs 1038ms (1000, 5.6×) — el margen se achica a mayor escala, reportado tal cual salió.
- **Test de paridad manual** (`scratchpad/parity_test.py`, no forma parte del repo): confirma que `POST /parse/python` y `_parse_python()` devuelven exactamente los mismos valores para todos los campos estructurales/numéricos sobre el mismo archivo de prueba (clase con método async, regex, recursión tail-call y no-tail, BFS) — la única diferencia es el idioma del texto de razón (inglés en Rust, español en Python, a propósito).
- 14 tests unitarios nuevos en Rust (`rich::tests`, mismos casos que ya existían en `test_intelligence.py`) + 2 en Python (`test_complexity_client.py`, degradación sin sidecar) + 5 en Python (`test_static_analysis.py`, nuevo — no existía cobertura para `/static/*` antes de esta pasada).

### Changed
- `routers/static_analysis.py::analyze_big_o` (`POST /static/bigO`) intenta primero `parse_python_rich()` para archivos `.py` — es un endpoint que genuinamente solo necesita `functions`, así que no pierde nada si el sidecar responde. Sin gate de flag cacheado: se intenta en vivo en cada pedido, misma lección de la condición de carrera de `complexity-engine` (v4.7.0). Cae a `parse_file()` (Python) si el sidecar no responde.
- **Deliberadamente NO conectado en `/static/parse`**: ese endpoint devuelve el shape legacy completo al frontend (`dead_code`, `call_graph`, `circular_deps`, `wasm_hints`, `exports`), ninguno de los cuales esta fase calcula todavía en Rust — conectarlo hubiera descartado en silencio campos que el panel Static renderiza. Queda documentado como el motivo concreto por el que `Dependency Engine`/WASM-hints/dead-code son la próxima porción lógica, no una elección arbitraria.
- `complexity.rs` gana una función pública `cyclomatic(body)` factorizada de `function_entry` (mismo comportamiento, cero cambio funcional) — para que `rich.rs` la reuse sin duplicar la lógica de McCabe una segunda vez.
- **`apps/terminal` y `apps/complexity` se mudan a `services/`**: distinción explícita entre productos que el usuario ejecuta directamente (`apps/api`, `apps/web`) y procesos/servidores independientes que esos productos consumen (`services/terminal`, `services/complexity`) — `apps/` dejaba de significar algo preciso apenas dejó de ser "todo lo que no es raíz". Movido con `git mv` (preserva historial). Sin cambios de comportamiento — actualizadas las rutas en `Cargo.toml` (5 `path =`), ambos `Dockerfile` (`COPY services/...`), `docker-compose.yml` (`dockerfile:`) y los comentarios de `scripts/run-{terminal,complexity}.mjs`; `scripts/build.{sh,ps1}` y CI no referenciaban paths directos, sin cambios ahí.

## [4.7.0] — 2026-08-17

### Added
- **Sidecar Rust `apps/complexity` (`complexity-engine`)**: reemplaza a `radon` — complejidad ciclomática (McCabe), Maintainability Index (fórmula Coleman-Oman) y métricas raw de líneas (loc/lloc/sloc/comments/blank/multi), calculadas con código propio sobre el AST de `rustpython-parser` en vez de importadas de una librería de terceros. Mismo patrón arquitectónico que `terminal-server` (v4.5): proceso persistente con servidor HTTP (axum), no subprocess-por-llamada ni extensión nativa PyO3 — descartado explícitamente porque el path de análisis se llama en cada pausa de tipeo del editor, y un subprocess ahí reintroduciría el costo de arranque de proceso que se optimizó para flake8/pylint en v4.6.0. Los dos sidecars ahora comparten un solo `Cargo.toml` en la raíz (2 `[[bin]]`, 1 `[lib]` nuevo — `complexity_core`, usado tanto por `main.rs` como por el benchmark).
- **`apps/api/services/complexity_client.py`** (nuevo): cliente HTTP async del sidecar (`analyze_complexity`, `check_complexity_engine`/`_sync`), con degradación con gracia (shape vacío, sin excepción) si el sidecar no está corriendo — mismo patrón de capacidad opcional que ya existía para flake8/pylint/radon vía `LIB_FLAGS`.
- **Benchmark con Criterion, no asumido**: `apps/complexity/benches/complexity_bench.rs` mide `complexity_core::analyze()` directo (sin HTTP) sobre archivos sintéticos de 10/100/1000 funciones, comparado contra `radon` real cronometrado con `timeit` sobre los mismos archivos. Resultado: 0.42ms vs 8.97ms (10 funciones), 4.69ms vs 89.2ms (100), 102ms vs 899ms (1000) — 9 a 21× más rápido. El motivo del cambio no fue rendimiento (era ownership de la lógica, no depender de una librería de terceros que no se controla) pero se midió igual antes de decidir qué decir acá, en vez de asumir.
- 15 tests unitarios en Rust (`cargo test`, corridos por el job `terminal` de CI, ahora renombrado porque compila/testea los dos sidecars) contra valores de complejidad/MI/raw calculados a mano sobre snippets canónicos. 4 tests nuevos en Python (`test_complexity_client.py`) que ejercitan la degradación con gracia real (el sidecar no corre durante `pytest` en CI — sin mocks, es el mismo comportamiento que vería un usuario que corre `pytest` sin haber hecho `npm run dev`).
- **Los 3 clasificadores pendientes del CS Engine (roadmap v4.4)**: Regex → Chomsky Tipo-3 (detecta llamadas directas `re.compile/match/search/...`), código con forma de gramática/parser → Chomsky Tipo-2 (heurística de nombre + recursión/pila explícita, ambas señales exigidas), y recorrido de grafo → BFS/DFS/Orden Topológico O(V+E) (heurística de nombres de variable `visited`/`in_degree` + forma de cola/pila). Mismo estilo de código que ya usan Big-O/Θ/Ω/recursión en `static_parser.py` — helpers de un solo `ast.walk` que devuelven señales, clasificadores que arman `(label, nota)` a partir de esas señales sin volver a recorrer el AST. Wireados en `/intel/analyze`, `/intel/hover` y la tabla de Big-O del panel Static (3 badges nuevos: 🔤 Regex, 🌳 CFG, 🕸️ BFS/DFS/Topo). 7 tests nuevos en `test_intelligence.py`, incluyendo guards de falso-positivo (una función recursiva sin nombre de parser no se clasifica como grammar-shaped).
- **Problems Panel conectado**, resolviendo la decisión de ubicación abierta desde v4.3/v4.6.0: nuevo 4to sub-tab del panel derecho (`#rpp-problems`/`#problems-content`) en vez de compartir contenedor con la vista de Análisis existente — sin ese cambio, wirear `updateProblems()` hubiera pisado contenido que esa vista tiene y Problems no (Pylint score, Maintainability Index, tabla de complejidad por función). Conectado en `editor.ts::applyMarkers()`, el punto de integración que el propio `panels/problems.ts` documentaba desde que se escribió.

### Removed
- 3 exports de TypeScript confirmados sin ningún caller, verificados dos veces (sin `onclick=`, sin `window.X`): `editor.ts::copyEditorContent`, `explorer.ts::explorerMarkModified`, `explorer.ts::explorerRefresh`. Una lista más larga de exports sin caller visible se encontró en la misma auditoría pero se dejó sin tocar a propósito — no hay certeza de que no sean superficie de API para algo que todavía no se conectó (mismo caso que tuvo el Force Graph antes de esta sesión).

### Changed
- `radon==6.0.1` eliminado de `requirements.txt`. Identificador de wire `"radon"` renombrado a `"complexity"` en toda la superficie donde era un string público — `tools: list[str]` de `/analyze/code`, `/analyze/project`, `/intel/analyze`, keys de capabilities (`LIB_FLAGS["HAS_RADON"]` → `HAS_COMPLEXITY_ENGINE`), y en el frontend (`types/index.ts`, `api/client.ts`, `components/app.ts` — ahí "radon" se mostraba literal como texto en el panel de capacidades, dejarlo habría mostrado un nombre que ya no es cierto — y las clases CSS `.t-radon`/`.pb-tool-radon` → `.t-complexity`/`.pb-tool-complexity`).

### Fixed
- **Condición de carrera en el chequeo de capacidad**, encontrada al conectar el sidecar: a diferencia de flake8/pylint (chequeo por `import`, determinístico), el chequeo de `complexity-engine` es de red — si `main.py` arranca antes de que `cargo build` termine de compilar el sidecar por primera vez (típico en un `npm run dev` en frío), el flag cacheado quedaba en `False` el resto de la sesión aunque el sidecar terminara de levantar segundos después. Arreglado quitando el gate `LIB_FLAGS["HAS_COMPLEXITY_ENGINE"]` de los call sites reales en `analysis.py` (`_run_complexity`/`_run_complexity_metrics` ya degradan con gracia por su cuenta vía `complexity_client`) — ahora cada análisis le pega al sidecar en vivo en vez de confiar en una foto de arranque. Verificado manualmente: bajar el sidecar, confirmar degradación sin crash, levantarlo de nuevo sin reiniciar el backend, confirmar que el siguiente análisis ya lo usa.
- De paso, corregidas dos versiones de Sythrall hardcodeadas como `v4.5` en `apps/api/main.py` (el log de arranque y el campo `server` de `/capabilities`), que habían quedado desactualizadas desde la v4.6.0.

## [4.6.0] — 2026-08-11

### Added
- **Un solo punto de entrada — Proyectos como hub**: "+ Código"/"+ Carpeta" del sidebar dejaron de crear archivos efímeros que se perdían al refrescar — ahora suman al proyecto activo (o crean uno nuevo, pidiendo nombre) usando el mismo backend que ya usaba el panel Proyectos. Antes había 4 entradas de archivos con 2 modelos mentales distintos (efímero vs persistido); ahora todo confluye en un proyecto real. `apps/api/routers/upload.py` gana `project_id` opcional en `/files` y `/folder` para hacer *append* a un proyecto existente en vez de crear uno nuevo cada vez. "+ Log" queda asociado al proyecto activo (`LogFile.projectId`).
- **Session Restore reconectado**: `panels/problems.ts` tenía `saveSession`/`restoreSession`/`clearSession` completos desde v4.3 pero sin un solo caller — no tenía sentido real hasta que hubo un proyecto persistido del cual recuperar contenido real, no solo un nombre de archivo. Ahora el proyecto activo se persiste en localStorage y, al volver a abrir la app, se restaura junto con el último archivo activo.
- **Live Metrics Bar reconectada**: mismo caso — `initLiveMetrics`/`updateLiveMetricsContent` (v4.3) nunca se llamaban desde `editor.ts`. Conectado siguiendo la integración de 2 líneas que el propio archivo ya documentaba.
- Métricas (`renderMetrics`) y badge de la pestaña APIs (`tb-apis`, nunca se actualizaba) también ganan los mismos modos/arreglos.
- **No conectado a propósito**: `updateProblems` (el "Problems Panel" VSCode-style de v4.3) escribe al mismo contenedor (`#analysis-content`) que `renderFileAnalysis`, que ya está en uso y tiene contenido que `updateProblems` no tiene (Pylint score, Maintainability Index, complejidad por función, botones de acceso a Diagrama/ML). Conectarlo tal cual pisaría esa información — falta decidir dónde vive realmente el Problems Panel antes de wirearlo.
- **Concepto de "proyecto activo"**: elegir o subir un proyecto en el panel Proyectos lo marca como activo (`state.activeProjectId`), y Editor/Issues/Diagrama/Static pasan a poder operar sobre él sin necesidad de cargar archivos a mano con "+ Código". Antes, abrir un archivo desde Proyectos solo pintaba el buffer de Monaco — nunca tocaba `state.files`, así que Issues/Diagrama/Static mostraban "sin archivos" aunque el código estuviera a la vista. El botón "Usar en editor" tampoco hacía nada más que un toast.
  - Backend: `apps/api/services/project_service.py` gana `read_project_files()` (factorizado del `os.walk` que antes vivía inline en `generate_project_graph`), y `POST /analyze/project` / `POST /static/parse-project` ahora aceptan `project_id` además de `files` inline — mismo patrón que ya usaba `POST /analyze/graph/project`. El backend lee del disco, el frontend no necesita mandar contenido de archivos.
  - Frontend: se conectó código de "Fase 2" que ya existía completo en `panels/graph.ts` (`generateProjectGraph`, `renderDirTree`, `openNodeInEditor`) pero no estaba wireado a ningún botón — de hecho, se descubrió que **todo** `panels/graph.ts` (incluyendo Import/Call/Circular/Heatmap graph, "Fase 1") no tenía ni un solo caller en el resto de la app. El panel Diagrama ahora tiene un grupo "Proyecto completo" en el selector de tipo, wireado al mismo pipeline de render Mermaid que ya usaba el flowchart de archivo único.
  - Pendiente, a propósito: el Force Graph interactivo y el árbol de directorios con complejidad por archivo (`renderForceGraph`/`renderDirTree`) tienen la lógica lista pero no la UI — la próxima pasada conecta esas dos piezas.
- **Sistema de iconos propio** (`apps/web/src/utils/icons.ts`): set de iconos SVG inline (`stroke="currentColor"`, heredan el color del tema activo — algo que un emoji no puede hacer) + `languageBadge()`, que reemplaza los mapas de emoji por extensión que estaban duplicados en 4 archivos (`upload.ts`, `explorer.ts`, `analysis.ts`, `problems.ts`, más `file-browser.ts` que los importaba).
- **Nav vertical persistente** reemplaza la tabbar horizontal de 11 pestañas — mismo espíritu que Aikido/Datadog/DeepSource (sidebar con iconos + secciones en vez de tabs de texto arriba). `switchTab()` y el wiring de clicks no cambiaron una línea: los items del nav nuevo mantienen `class="tab"` + `data-tab` + `id="t-*"`, así que es un cambio de HTML/CSS, no de lógica.

### Changed
- **Renombrado de CodeWatch PRO a Sythrall**: nombre nuevo en todo el proyecto — `package.json`, `Cargo.toml` (`sythrall-terminal`), `docker-compose.yml` (`sythrall-backend`/`sythrall-frontend`/`sythrall-terminal`/`sythrall-net`), identificadores internos (tema de Monaco, owner de markers, `SESSION_KEY`, `META_FILENAME` del caché de proyectos, loggers), remote de git. Las entradas viejas del changelog que mencionan `CodeWatch PRO` o `.codewatch-meta.json` quedan como estaban — son registro histórico de cómo se llamaba el proyecto en esas versiones, no se reescriben retroactivamente.
- **Reestructuración a `apps/`**: `backend/` → `apps/api/`, `frontend/` → `apps/web/`, `terminal-server/` → `apps/terminal/`. Los manifiestos de cada herramienta (`package.json`, `pyproject.toml`, `Cargo.toml`, `vite.config.ts`, `tsconfig.json`, `biome.json`, `docker-compose.yml`, CI) se quedan en la raíz — ya funcionaban así antes del cambio (p.ej. `vite.config.ts` ya apuntaba a `frontend/` como `root` en vez de vivir adentro). Solo se agrupó el código de cada app bajo un directorio común, siguiendo la convención de monorepos tipo Turborepo/Nx en vez de tener tres carpetas de app sueltas al tope del repo. No afecta a ninguna funcionalidad ni al motor de análisis — es puramente organizativo, con cada referencia de path (Docker, scripts, CI, `.gitignore`) actualizada en consecuencia.

### Fixed
- **`/analyze/graph` (Import Graph) era O(archivos²) por un bug escondido en un one-liner**: `_build_import_graph` en `apps/api/routers/graph.py` reconstruía `{e["from"] for e in edges}` *adentro* del `sum(...)` que calcula archivos aislados — se reconstruía en cada iteración del loop de nodos en vez de una sola vez. Con 4003 archivos sintéticos (benchmark reproducible, ver más abajo) pasó de 3.9s a 0.128s — 30x — al sacar el set-comprehension del loop, igual que ya estaba hecho con `targets`.
- Los 4 generadores de Mermaid (import graph, call graph, circular dependencies, complexity heatmap) en `apps/api/routers/graph.py` concatenaban el string con `mermaid += f"..."` dentro de loops — 17 ocurrencias — reemplazado por acumular en lista + `"\n".join()` al final.
- `/static/parse-project` calculaba un `dependency_graph` con loop triple O(archivos²) que **el frontend nunca leía** (confirmado: no aparece en ningún componente de `apps/web/src`, ni en tests) — con 4003 archivos tardaba 48s calculando algo que se descartaba. Eliminado junto con `all_imports`/`all_exports`, la misma función, también muertos.

### Performance
- Motivado por la ambición de que Sythrall aguante proyectos gigantes: en vez de asumir que hacía falta reescribir el motor en otro lenguaje, se armó un benchmark reproducible (proyecto sintético de hasta 4003 archivos + archivos individuales de hasta 1600 funciones) que aisló los tres bugs de arriba — los tres eran algorítmicos (O(N²) evitable), ninguno tenía que ver con las limitaciones reales de Python. El motor de parsing propio (`parse_file`) ya escalaba lineal antes de este cambio y sigue igual; el cuello de botella estaba en código alrededor del parser, no en el parser.

## [4.5.0] — 2026-08-01

### Added
- **Terminal integrada, estilo VSCode**: shell interactiva real (PowerShell en Windows, `$SHELL`/bash en Unix) embebida en un panel inferior redimensionable, con botón dedicado en el topbar. Primera vez que el proyecto usa **Rust**: nuevo sidecar `terminal-server` (`portable-pty` + `axum` + `tokio`), corriendo como tercer proceso de `npm run dev` (`[web]`/`[api]`/`[term]`). `portable-pty` da una sola implementación para ConPTY (Windows) y PTY (Unix) — evita mantener dos code paths distintos con sus propias rarezas de encoding/resize.
  - **Seguridad**: token aleatorio de 32 bytes (`OsRng`, comparación en tiempo constante vía `subtle::ConstantTimeEq`) generado por arranque, exigido antes de aceptar el WebSocket. El sidecar bindea a `127.0.0.1` por defecto (a diferencia del resto de la app), overridable por env var (`TERMINAL_HOST`/`TERMINAL_PORT`) solo si se lo expone intencionalmente.
  - **Auto-conexión sin fricción**: para uso local normal no hace falta pegar el token a mano — el sidecar lo sirve automáticamente (`GET /terminal/token`) solo a pedidos que verificablemente vienen de la misma máquina (chequeo de `X-Forwarded-For` a través del proxy de Vite, tomando el último valor de la cadena — el único que el proxy controla y el cliente no puede falsificar). Si el token es inválido o el pedido no es local, cae al modal de pegar el token manualmente.
  - Panel con selector **Terminal / Logs**: alterna entre la shell interactiva y una vista de solo lectura con el mismo stream de eventos del tab "Logs" de siempre, en tiempo real. La sesión de shell sigue viva en segundo plano al cambiar de vista.
- **Tema claro/oscuro**: toggle en el topbar, oscuro sigue siendo el default, persiste en `localStorage`, sin flash del tema incorrecto al recargar.
- **Explorador de carpetas en el sidebar** ("+ Carpeta", junto a "+ Código"/"+ Log"): árbol expandible/colapsable estilo VSCode a partir de una carpeta real del disco, vía `<input webkitdirectory>` — funciona en Chrome, Edge, Firefox y Safari (11.1+), sin depender de la File System Access API (que es solo Chromium). El árbol se arma 100% en el cliente; abrir un archivo lo integra al mismo pipeline de análisis que "+ Código".
- **[`ansimax`](https://github.com/Brashkie/ansimax)** (librería propia) integrada en los scripts Node de `npm run dev`: banner con gradiente al arrancar y mensajes de error con color/box.

### Performance
- El bundle de `@xterm/xterm` (~330KB) queda con carga diferida — no se descarga hasta que el usuario abre el panel de Terminal por primera vez (`import()` dinámico, vendor chunk propio en `vite.config.ts`). El chunk principal del bundle no creció.

### Fixed
- `backend/tests/test_static_analysis.py` estaba vacío (0 líneas, 0 tests) — resto de una limpieza anterior, nunca se borró. Sus casos ya viven en `test_intelligence.py`/`test_static_parser` de otros archivos; no se perdió cobertura al borrarlo.
- Números de versión inconsistentes dentro de `backend/main.py` (`v4.1`, `4.2.0`, `v4.2` en tres lugares distintos, ninguno coincidía con `package.json`) — unificados a la versión real del proyecto.
- Conteo de tests desactualizado en el README (decía 316, con un archivo vacío incluido en la cuenta) — corregido a los 297 reales.
- `release.yml` (el workflow que corre al pushear un tag) no compilaba ni testeaba el sidecar Rust — un tag se podía publicar aunque `terminal-server` no compilara. Se agregó el mismo job de `cargo build/clippy/test` que ya tiene `ci.yml`.

## [4.4.0] — 2026-07-31

### Added
- **Computer Science Engine (parcial)**: Θ (cota ajustada) y Ω (mejor caso) junto al O ya existente, para funciones Python — se calculan detectando `break`/`return` dentro de un loop (si existe, el mejor caso es Ω(1), como una búsqueda lineal que encuentra el elemento de una). Visible en la tabla Big-O del panel Static Analysis y en el hover del editor.
- Textos de "razón" del Big-O reescritos para ser explicativos ("2 loops anidados — el loop interno se ejecuta n veces por cada iteración del externo") en vez de etiquetas terse.
- Detección de **tail-call recursion** para funciones Python: distingue `return f(n-1)` (tail, equivalente a un loop) de `return n + f(n-1)` (no-tail, cada nivel consume stack) — con framing de Cálculo Lambda. Badge 🔁 en la tabla Big-O y fila extra en el hover del editor cuando la función es recursiva.
- Carpeta `scripts/`: `setup`/`dev`/`build`/`test`/`lint`/`format`, cada uno en `.ps1` (Windows) y `.sh` (bash) — flujo de desarrollo local completo sin depender de Docker.
- `npm run dev` levanta backend (uvicorn) y frontend (vite) juntos vía `concurrently`, con logs `[web]`/`[api]` prefijados en una sola consola.
- Biome (frontend) y Ruff (backend) adoptados para lint/format.
- `.dockerignore`, `.vscode/extensions.json`, `CHANGELOG.md`.
- `.github/workflows/ci.yml` — typecheck/lint/build (frontend) + ruff/pytest (backend) en cada push/PR a `main`.
- `.github/workflows/release.yml` — al pushear un tag `vX.Y.Z`, corre la misma verificación y crea una GitHub Release con las notas extraídas de `CHANGELOG.md` y el build del frontend como artefacto adjunto.

### Performance
- **`GET /api/upload/projects` era el cuello de botella real detrás del lag general de la app**: recorría el disco completo (`rglob` + `stat` de cada archivo) de *cada* proyecto subido acumulado, de forma síncrona, bloqueando el servidor entero mientras corría. Ahora la metadata de cada proyecto se calcula una vez al subirlo y se cachea en `.codewatch-meta.json` (excluido del árbol de archivos que ve el usuario); listar proyectos solo lee esos caches. Con ~480 proyectos acumulados, la respuesta pasó de bloquear el server a ~30ms.
- Subida de ZIPs grandes (extracción + armado del árbol) movida a threadpool (`starlette.concurrency.run_in_threadpool`) — ya no bloquea el event loop para el resto de las requests mientras procesa un ZIP grande. Mismo tratamiento para listar/borrar proyectos.
- Límite de retención: se guardan como máximo 20 proyectos subidos — al subir uno nuevo que supere el tope, se borran automáticamente los más viejos. Antes no había límite: se habían acumulado ~480 proyectos de prueba (595 MB) sin ningún mecanismo de limpieza.
- Frontend: el árbol de archivos ya no renderiza carpetas enteras de una si tienen cientos/miles de hijos (tope de 300 por carpeta, con indicador "+N más") — evita trabar el navegador al ver un proyecto muy grande.
- **Detección de dependencias circulares (`/analyze/graph`, tipo `circular`) podía colgar el servidor**: `nx.simple_cycles()` se convertía a `list()` completo antes de usarlo, forzando enumerar *todos* los ciclos simples del grafo — en un proyecto de ~150 archivos con solo 3% de densidad de imports ya son 200.000+ ciclos posibles. Confirmado con benchmark: ese caso pasó de 1088ms (sin terminar de crecer) a 4.46ms cortando la enumeración apenas se encuentran 20 ciclos (`find_cycles_capped()`, nueva función compartida en `services/static_parser.py`, usada también por `routers/graph.py`) — de cualquier forma solo se muestran los primeros, así que no se pierde nada útil. No fue necesario reemplazar Python/NetworkX por Rust: el problema era enumeración sin límite, no velocidad de lenguaje — cualquier implementación tiene el mismo crecimiento exponencial sin un tope.

### Changed
- Todos los manifiestos/configs (`package.json`, `requirements.txt`, `pyproject.toml`, `biome.json`, `pytest.ini`) se movieron a la raíz del repo; `backend/` y `frontend/` ahora solo contienen código fuente.
- `vite.config.ts`: `root: 'frontend'`, el build ahora escribe en un `dist/` de la raíz.
- Contexto de build de Docker cambiado a la raíz del repo para ambos servicios (`docker-compose.yml` y ambos `Dockerfile` actualizados) — **no probado con un build real** (sin CLI de Docker disponible en el entorno de desarrollo usado); verificar con `docker compose build` antes de confiar en ese camino.
- Badge/footer de licencia corregido de Apache-2.0 a GPL-3.0, para que coincida con el archivo `LICENSE` real.
- Roadmap del README reorganizado: las secciones ahora agrupan por qué tan fundamentada está cada idea (lista para construir vs. "integrar, no reconstruir" vs. largo plazo/otra categoría de herramienta), en vez de una lista plana por versión.

### Fixed
- **Big-O incorrecto para loops anidados en TS/JS** (`/metrics/live`) — la heurística por regex confundía "cantidad de coincidencias del patrón de anidamiento" con "cantidad de loops", dando `O(n)` para código con loops anidados en vez de `O(n²)`. Detectado por el propio CI (`ci.yml`) en su primera corrida.
- **Explorer panel sin estilos** — `main.css` nunca importaba `explorer.css`.
- **Archivos duplicados al subir** — soltar/seleccionar un archivo agregaba dos entradas idénticas; causado por `app.ts` y `events.ts` cableando ambos los mismos listeners de `drop`/`change` (resto de un refactor incompleto).
- **Topbar desbordado en anchos de laptop comunes (~1100–1650px)** — demasiados elementos `flex-shrink:0` para el espacio disponible; se agregó un breakpoint dedicado que oculta chips/atajos secundarios antes de que el botón principal "Analizar" quede fuera de pantalla.
- Crash de consola en Windows al arrancar (`UnicodeEncodeError` al imprimir logs con emojis bajo `cp1252`) — `shared.py` ahora fuerza UTF-8 en stdout/stderr.
- `backend/routers/history.py` eliminado — código muerto, nunca registrado, reemplazado por `routers/logs.py`.
- Código muerto vario eliminado: implementaciones duplicadas de `_get_lib_version`/`_cyclomatic_python`, `safe_remove`/`save_temp` redefinidos tapando su propio import, imports/variables sin uso en ~10 archivos (frontend y backend).
- CSS duplicado/muerto: `main_upload_addon.css` (no usado, tenía las correcciones que a `upload.css` le faltaban) fusionado y eliminado; tres `nginx.conf` muertos eliminados (`./nginx.conf`, `frontend/nginx.conf`, `docker/nginx.conf` — ninguno estaba referenciado en ningún lado).

---

## [4.3.0] — Problems Panel + Live Metrics
### Added
- Panel de problemas (estilo VSCode): errores, warnings, Big-O, complejidad, hallazgos de seguridad.
- Barra de métricas en vivo en el editor: LOC, funciones, imports, complexity score, Big-O peor caso, parse time.
- Safe mode: fallback por regex + auto-recovery cuando el parser AST falla; detección de archivos corruptos, restauración de sesión.
### Changed
- Licencia cambiada a GPL-3.0.

## [4.2.0] — Code Graph + Project Explorer
### Added
- Import Graph, Call Graph, Dependencias Circulares, Complexity Heatmap (Tree View + Force Graph interactivo).
- Project Explorer: árbol de archivos, tabs multi-archivo, búsqueda global, outline de símbolos.
- 316 tests automatizados.

## [4.1.0] — Static Analysis + Editor Intelligence
### Added
- Parser AST multi-lenguaje (Python, TypeScript, C/C++), sin IA.
- Estimación Big-O, complejidad ciclomática, hints WASM/Cython.
- Linting en tiempo real, hover, Go to Definition, Find References, Rename Symbol, autocompletado semántico.

## [4.0.0] — FastAPI + Project Upload
### Changed
- Migración del backend de Flask a FastAPI.
### Added
- Upload de proyectos (archivos/carpetas/ZIP).

## [3.0.0] — Zoom/Pan + Responsive
### Added
- Zoom y pan en diagramas; layout responsive con bottom navigation en móvil.

## [2.0.0] — ML/DL Inspector
### Added
- Detección de 23 librerías, 23 patrones de pipeline, 25 modelos, 20+ reglas de issues.
- Score 0–100 + diagrama Mermaid del pipeline.

## [1.0.0] — Foundation
### Added
- Backend Flask + pylint/flake8/radon.
- Frontend TypeScript + Vite (sin frameworks), Monaco Editor, Chart.js, Docker.
